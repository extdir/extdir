# define your env variables for the test env here
KERNEL_CLASS='App\Kernel'
APP_SECRET='$ecretf0rt3st'

# Deliberately fictitious. The real address is a private individual's home and
# lives only in .env.local on the server; tests assert the plumbing, not the data.
OPERATOR_NAME='Test Operator'
OPERATOR_STREET='Teststraße 1'
OPERATOR_POSTAL_CITY='12345 Teststadt'
OPERATOR_COUNTRY='Germany'
